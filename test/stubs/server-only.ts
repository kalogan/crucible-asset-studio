// Test stub: `server-only` throws when imported outside a React Server Component.
// In vitest we alias it to this no-op so we can unit-test the PURE helpers exported
// from server-only modules (the runtime guard still applies in the real app).
export {};
