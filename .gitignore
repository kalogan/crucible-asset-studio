# Dependencies
node_modules/
.pnp
.pnp.js

# Reference source — local-only backstop (carries its own nested .git, and
# KERNEL_LESSONS.md is the committed authority). Kept on disk, excluded from the repo.
_reference/

# Env & secrets
.env
.env.local
.env.*.local
*.local

# Local tooling (portable Blender for the auto-rig pipeline, etc.) — fetched by a
# setup script, never committed (large binaries; version pinned in the script).
tools/

# Build output
dist/
build/
.next/
out/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Python bytecode
__pycache__/
*.pyc
